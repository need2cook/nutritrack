[pytest]
testpaths = tests
asyncio_mode = auto
pythonpath = . app

addopts = 
    -v
    -s
    --tb=short
    --color=yes
    --strict-markers

markers =
    unit: Unit tests
    integration: Integration tests (require database)
    slow: Slow running tests
    asyncio: Async tests